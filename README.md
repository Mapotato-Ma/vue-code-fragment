# vue-code-fragment

用 Vue 写的一些小 demo。主应用在 **`vue-demos/`**；轻量 API 在 **`server/`**（**TypeScript** + `data/pens.json`，`GET /api/code-pens`，编译后 `node dist/index.js`）。详见各子目录 `README.md` 与 [`docs/项目亮点-AI阅读.md`](docs/项目亮点-AI阅读.md)。

